interface:
  display_name: "Product Playbook"
  short_description: "Create and reconcile tester-facing playbooks"
  default_prompt: "Use $product-playbook with one or more local paths or repository URLs to create or reconcile a portable tester-facing playbook."
