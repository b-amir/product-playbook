interface:
  display_name: "Product Playbook"
  short_description: "Inspect, reconcile, and agent-check tester-facing playbooks"
  default_prompt: "Use $product-playbook. Follow Intake Card + one poll round (prefer structured choices), then Plan, then Write only after approval. Keep the playbook tester-only (Markdown + one state JSON). Put Agent-check findings in sibling playbook-findings/ only when asked."
