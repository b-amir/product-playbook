interface:
  display_name: "Product Playbook"
  short_description: "Inspect and reconcile tester-facing playbooks"
  default_prompt: "Use $product-playbook to inspect this workspace, summarize repositories and prior evidence, and ask me to confirm assumptions before editing."
