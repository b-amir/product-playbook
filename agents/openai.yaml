interface:
  display_name: "Product Playbook"
  short_description: "Create and reconcile tester-facing product playbooks"
  default_prompt: "Use $product-playbook to create or reconcile a tester-facing playbook. Ask for output_dir when multiple repos or docs roots make the destination ambiguous. Keep authoring metadata out of the published Markdown."
